using DAO_MVC_Singleton.Control;
using System;
using System.Data;
using static System.Console;

namespace DAO_MVC_Singleton
{
    internal class Program
    {
        static void Main(string[] args)
        {
            ControlAll controlAll = new ControlAll();
            ReadKey();
        }
    }
}
